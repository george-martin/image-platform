services:
  upload-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - image-platform
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

networks:
  image-platform:
    external: true